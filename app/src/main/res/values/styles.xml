<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Material.Dialog.Alert">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">#F7F7F7</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="AlertDialogCustom_NoActionBar" parent="@android:style/Theme.Material.Dialog.NoActionBar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">#F7F7F7</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="SwitchCompatTheme" >
        <item name="trackTint">@drawable/selector_switch</item>
    </style>

</resources>
